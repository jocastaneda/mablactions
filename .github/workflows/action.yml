on: [workflow_dispatch,pull_request]

name: Runnning Test inside of CI/CD

jobs:
  test:
    name: mabl Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-node@v2
        with:
          node-version: '20.x'

      - uses: browser-actions/setup-chrome@v1
        with:
          chrome-version: 130
        
      - uses: mablhq/setup-mabl-cli@v1
        with:
          workspace-id: ${{ secrets.WORKSPACE_ID }}
        env:
          MABL_API_KEY: ${{ secrets.CLI_API_KEY }}

      - name: Run the test headlessly
        run: mabl tests run --id gmG3rjgoTaCTSJY44lze4g-j --environment-id uKI949gWxPdWWsCY7dFv8g-e --url "https://sandbox.mabl.com" --headless
